[tool.poetry]
name = "oblig3"
version = "0.1.0"
description = "OBLIG3"
authors = ["N"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
pytest = "^7.4.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
